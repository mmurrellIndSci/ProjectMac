using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace Fortive.Mac
{
    public enum AlarmLevel
    {
        None,
        GasDetected,
        LowAlarm,
        HighAlarm,
        PeerAlarm,
        PanicAlarm,
    }
}
